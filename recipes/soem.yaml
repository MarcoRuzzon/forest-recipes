clone:
  type: git
  server: github.com
  repository: ADVRHumanoids/soem_tree.git
  tag: feature/AoE

build:
  type: cmake
  args: []
  args_if:
    xeno:
      - -DCMAKE_USE_XENOMAI=ON
      - -DCMAKE_BUILD_TYPE=None

depends: []
