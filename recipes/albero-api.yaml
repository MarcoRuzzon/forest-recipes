clone:
  type: git
  server: github.com
  repository: ADVRHumanoids/albero-api.git
  tag: cartesio2

build:
  type: custom
  cmd:
    - cd {srcdir} && pip2 install --user -e .

depends:
  - python-tk
  - cartesian_interface
  - tf