clone:
  type: git
  server: github.com
  repository: ADVRHumanoids/modular.git
  tag: feature/iolink

build:
  type: custom
  cmd:
    - cd {srcdir} && pip2 install --user -e .

depends:
  - tf
  - ec_xbot2_client